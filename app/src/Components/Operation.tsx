import Typography from '@mui/material/Typography';

const Operation = () => {
    return (
        <>
            <Typography paragraph>
                ここに設定画面を実装する。
            </Typography>
        </>
    );
};

export default Operation;