import Typography from '@mui/material/Typography';

const Minutes = () => {
    return (
        <Typography paragraph>
            Minutes is desplayed.
        </Typography>
    );
};

export default Minutes;